import React from 'react';

/**
 * Placeholder Toaster component to resolve import errors
 * This is a temporary fix until proper shadcn/ui components are installed
 */
export const Toaster = () => {
  return null; // Renders nothing, just satisfies the import
};

export default Toaster;